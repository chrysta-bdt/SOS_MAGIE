<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(https://images.unsplash.com/photo-1594396555192-a411562137eb?ixlib=rb-1.2.1&ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&auto=format&fit=crop&w=2070&q=80);">
  <div class="container">
    <h1>SOS MAGIE</h1>
    <p> Il n'y a pas de problème, que des solutions.</p>
  </div>
</div>

<div class="container">
  <div class="row">
  <a class="btn btn-primary" href="/params[id]">Catégorie Love</a>

    <% @love_services.each do |love_service| %>
      <div class="cards">

        <%= render 'services/card', service: love_service %>

        <%= render 'services/card', service: love_service %>

      </div>

    <% end %>
</div>

  <div class="row">
    
   <a class="btn btn-primary" href="/params[id]">Catégorie Theft</a>
    <h3>Catégorie Theft<h3>
    <% @theft_services.each do |theft_service| %>
      <div class="cards">
        <%= render 'services/card', service: theft_service %>

        <%= render 'services/card', service: theft_service %>
      </div>
    <% end %>
  </div>

  <div class="row">
    
   <a class="btn btn-primary" href="/params[id]">Catégorie Conflict</a>
    <h3>Catégorie Conflict<h3>

    <% @conflict_services.each do |conflict_service| %>
       <div class="cards">
        <%= render 'services/card', service: conflict_service %>

        <%= render 'services/card', service: conflict_service %>
      </div>
    <% end %>
  </div>


<div class="row">
    

  <div class="row">
   <a class="btn btn-primary" href="/params[id]">Catégorie money</a>


    <% @money_services.each do |money_service| %>
       <div class="cards">
        <%= render 'services/card', service: money_service %>

        <%= render 'services/card', service: money_service %>
      </div>
    <% end %>
  </div>
</div>
